# @algorisys/zen-ui-solid (dist branch)

Prebuilt distribution of `packages/solid` for direct git installation:

```json
"@algorisys/zen-ui-solid": "github:Algorisys-Technologies/zen-ui#release/solid"
```

Do not edit this branch by hand — it is regenerated from `main` via
`bun run build:lib:solid` (see packages/solid). Source of truth is `main`.
